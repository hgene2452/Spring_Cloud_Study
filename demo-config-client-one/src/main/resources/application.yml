spring:
  application:
    name: application

  profiles:
    active: oauth-dev

  config:
    import: optional:configserver:http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans